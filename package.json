{
  "name": "echohook-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
