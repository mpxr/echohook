{
  "name": "echohook",
  "version": "0.0.1",
  "description": "Echohook - the future of testing with webhooks",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --env development",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --config vitest.coverage.config.ts --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20250604.0",
    "@vitest/coverage-v8": "2.1.9",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.19.1"
  },
  "dependencies": {
    "hono": "^4.7.11"
  }
}
